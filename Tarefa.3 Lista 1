#include <iostream>
using namespace std;

int main(){
	
	int valor, parcelas, total;
	
 	cout << "Ola, voce está comprando em a SUAS MELHORES COMPRAS\n";
 	cout << "Digite o valor da sua compra: ";
    cin >> valor;
    
    cout << "Digite em quantas parcelas deseja dividir: ";
    cin >> parcelas;
    
    total= valor/parcelas;
    
    cout << "Voce devera pagar: " << total << " por mes!";
 	}
